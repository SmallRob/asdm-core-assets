name: Package Skills

on:
  push:
    branches:
      - main
    paths:
      - 'asdm-core-assets/skills/**'
  pull_request:
    branches:
      - main
    paths:
      - 'asdm-core-assets/skills/**'
  workflow_dispatch:

jobs:
  package-skills:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create zip files for each skill
        run: |
          SKILLS_DIR="asdm-core-assets/skills"
          OUTPUT_DIR="$GITHUB_WORKSPACE/packaged-skills"
          
          # Create a directory to store zip files
          mkdir -p "$OUTPUT_DIR"
          
          # Loop through each subdirectory in skills
          for dir in "$SKILLS_DIR"/*/; do
            if [ -d "$dir" ]; then
              # Get the directory name
              dirname=$(basename "$dir")
              echo "Packaging skill: $dirname"
              
              # Create zip file
              (cd "$SKILLS_DIR" && zip -r "$OUTPUT_DIR/${dirname}.zip" "$dirname")
            fi
          done
          
          # List created zip files
          echo "Created zip files:"
          ls -la "$OUTPUT_DIR/"

      - name: Upload skill artifacts
        uses: actions/upload-artifact@v4
        with:
          name: skills-packages
          path: packaged-skills/*.zip
          retention-days: 30

      # Deploy to SIT01 environment
      - name: Deploy to SIT01
        if: github.ref == 'refs/heads/main'
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_PORT: ${{ secrets.SSH_PORT }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_PASSWORD: ${{ secrets.SSH_PASSWORD }}
        run: |
          set -e

          sudo apt-get update
          sudo apt-get install -y openssh-client rsync sshpass
          
          # Remote paths
          ARTIFACTS_DIR="/data/artifacts"

          echo "Using SSH port: $PORT"
          
          # Create remote directory
          sshpass -p "$SSH_PASSWORD" ssh -o StrictHostKeyChecking=no -p $SSH_PORT $SSH_USER@$SSH_HOST "mkdir -p $ARTIFACTS_DIR/skills"
            
          # Upload files
          sshpass -p "$SSH_PASSWORD" rsync -avz --delete \
            -e "ssh -o StrictHostKeyChecking=no -p $SSH_PORT" \
            packaged-skills/ \
            $SSH_USER@$SSH_HOST:$ARTIFACTS_DIR/skills/
          
          echo "Deployed toolsets to SIT01 successfully"

      - name: Deploy to DT01
        if: github.ref == 'refs/heads/main'
        env:
          SSH_HOST: ${{ secrets.SSH_HOST_DT }}
          SSH_PORT: ${{ secrets.SSH_PORT }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_PASSWORD: ${{ secrets.SSH_PASSWORD }}
        run: |
          set -e

          sudo apt-get update
          sudo apt-get install -y openssh-client rsync sshpass
          
          # Remote paths
          ARTIFACTS_DIR="/data/artifacts"

          echo "Using SSH port: $PORT"
          
          # Create remote directory
          sshpass -p "$SSH_PASSWORD" ssh -o StrictHostKeyChecking=no -p $SSH_PORT $SSH_USER@$SSH_HOST "mkdir -p $ARTIFACTS_DIR/packaged-skills"
            
          # Upload files
          sshpass -p "$SSH_PASSWORD" rsync -avz --delete \
            -e "ssh -o StrictHostKeyChecking=no -p $SSH_PORT" \
            packaged-skills/ \
            $SSH_USER@$SSH_HOST:$ARTIFACTS_DIR/skills/
          
          echo "Deployed toolsets to DT01 successfully"

      - name: Deploy to DT02
        if: github.ref == 'refs/heads/main'
        env:
          SSH_HOST: ${{ secrets.SSH_HOST_DT02 }}
          SSH_PORT: ${{ secrets.SSH_PORT }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_PASSWORD: ${{ secrets.SSH_PASSWORD }}
        run: |
          set -e

          sudo apt-get update
          sudo apt-get install -y openssh-client rsync sshpass
          
          # Remote paths
          ARTIFACTS_DIR="/data/artifacts"

          echo "Using SSH port: $PORT"
          
          # Create remote directory
          sshpass -p "$SSH_PASSWORD" ssh -o StrictHostKeyChecking=no -p $SSH_PORT $SSH_USER@$SSH_HOST "mkdir -p $ARTIFACTS_DIR/packaged-skills"
            
          # Upload files
          sshpass -p "$SSH_PASSWORD" rsync -avz --delete \
            -e "ssh -o StrictHostKeyChecking=no -p $SSH_PORT" \
            packaged-skills/ \
            $SSH_USER@$SSH_HOST:$ARTIFACTS_DIR/skills/
          
          echo "Deployed toolsets to DT02 successfully"

      - name: Deploy to PROD
        if: github.ref == 'refs/heads/main'
        env:
          SSH_HOST: ${{ secrets.SSH_HOST_PROD }}
          SSH_PORT: ${{ secrets.SSH_PORT }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_PASSWORD: ${{ secrets.SSH_PASSWORD }}
        run: |
          set -e

          sudo apt-get update
          sudo apt-get install -y openssh-client rsync sshpass
          
          # Remote paths
          ARTIFACTS_DIR="/data/artifacts"

          echo "Using SSH port: $PORT"
          
          # Create remote directory
          sshpass -p "$SSH_PASSWORD" ssh -o StrictHostKeyChecking=no -p $SSH_PORT $SSH_USER@$SSH_HOST "mkdir -p $ARTIFACTS_DIR/skills"
            
          # Upload files
          sshpass -p "$SSH_PASSWORD" rsync -avz --delete \
            -e "ssh -o StrictHostKeyChecking=no -p $SSH_PORT" \
            packaged-skills/ \
            $SSH_USER@$SSH_HOST:$ARTIFACTS_DIR/skills/
          
          echo "Deployed toolsets to PROD successfully"
